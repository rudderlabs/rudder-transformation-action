name: Test

on: [push, pull_request]

jobs:
  request:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}
      - name: Test transformers and libraries
        uses: ./
        with:
          metaPath: './code/meta.json'
          email: 'sayan+10@rudderlabs.com'
          accessKey: '1q6uMVEyyRHTPXvk7VIcT0NoISU'
          serverEndpoint: 'https://ccef2688f00e.ngrok.io'
          uploadTestArtifact: true